apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: portfolio-db
  namespace: portfolio
spec:
  instances: 3
  
  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "256MB"
      effective_cache_size: "1GB"
      maintenance_work_mem: "64MB"
      checkpoint_completion_target: "0.9"
      wal_buffers: "16MB"
      default_statistics_target: "100"
      random_page_cost: "1.1"
      effective_io_concurrency: "200"
      work_mem: "2621kB"
      min_wal_size: "1GB"
      max_wal_size: "4GB"
  
  storage:
    size: 10Gi
    storageClass: standard
  
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"
  
  # Primary update strategy
  primaryUpdateStrategy: unsupervised
  
  # Backup configuration (optional - uncomment if needed)
  # backup:
  #   barmanObjectStore:
  #     destinationPath: s3://portfolio-db-backups/
  #     s3Credentials:
  #       accessKeyId:
  #         name: backup-creds
  #         key: ACCESS_KEY_ID
  #       secretAccessKey:
  #         name: backup-creds
  #         key: ACCESS_SECRET_KEY
  #   retentionPolicy: "30d"
  
  # Monitoring
  monitoring:
    enablePodMonitor: true
  
  # Bootstrap from scratch (initdb)
  bootstrap:
    initdb:
      database: portfolio
      owner: portfolio